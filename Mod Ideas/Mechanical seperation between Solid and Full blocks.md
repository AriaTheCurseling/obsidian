Currently, redstone only cares if a block is *transparent* or *solid*. 

this would let a comparator interact with an inventory through glass, as glass is a full transparent block (where currently comparators care about solidness instead of fullness)